{
  "name": "docker-demo-app",
  "version": "1.0.0",
  "description": "Demo app: custom Docker image (Node/Express) + public Docker image (MongoDB)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.5.0"
  }
}
